// EXPECT ERROR: does not implement action
interface I {
    int mustImplement();
    int optional() { return 1; }
}
class C {
    interface class I;
    action:
        // 缺 mustImplement() → 必须实现；optional() 可省略（有默认体）
}

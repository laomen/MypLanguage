// BUG-115 负向：枚举变体数据实参数漏校验（Opt.Some(1,2) 多参静默被忽略）。
// 镜像 C++ — 带数据变体是 Function 类型，normalizeCallArgs 严格计数。
// EXPECT ERROR: expected 1 arguments, got 2
import env;
enum Opt { Some(int value); None; }
class T2 {
    action:
        @constructor T2() {
            Opt o = Opt.Some(1, 2);
            Console.writeString("A\n");
        }
}
int main(){T2 t=new T2();return 0;}

// Negative: string concatenation inside @parallel for body.
// Previously emitted garbage pointer arithmetic → LLVM verify crash
// ("Call parameter type does not match function signature!"). Rejected now.
import env;
import pool;

class Main {
    action:
        @startup void run() {
            int n = 4;
            @parallel for (int i = 0; i < n; i = i + 1) {
                // ERROR: string operations not supported inside @parallel for body
                string s = "iter " + i;
            }
        }
}

int main(){ Main m = new Main() @thread; return 0; }

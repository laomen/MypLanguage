// BUG-102 负向：bitvector 位运算须同宽（镜像 C++ sema_expr.cpp visitBinaryOp
// "bitvector bitwise operations require both operands to be bitvector<N> of the same width"）
// EXPECT ERROR: bitvector bitwise operations require both operands to be bitvector<N> of the same width
import env;
class T2 {
    action:
        @constructor T2() {
            bitvector<8> a = bitvector<8>(5);
            bitvector<16> b = bitvector<16>(1);
            bitvector<8> c = a & b;
            Console.writeString("A\n");
        }
}
int main(){T2 t=new T2();return 0;}

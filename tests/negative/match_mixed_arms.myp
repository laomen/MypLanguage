// EXPECT ERROR: cannot mix enum variant and literal match arms
// match 扩展：枚举臂与字面量臂混用 → 编译拒绝。
enum E { V0; V1; }
class T {
    action:
        void f(int x) {
            match (x) {
                E.V0 => { }
                1 => { }
            }
        }
        @constructor T() { this.f(1); }
}
int main(){return 0;}
